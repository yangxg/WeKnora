# ResearchFlow Customizations

This ledger records every ResearchFlow-specific divergence from Tencent/WeKnora upstream on the `researchflow-ext` branch.

## Upstream baseline

- Synchronized on: 2026-08-05 (W3 opening sync)
- Upstream remote: `https://github.com/Tencent/WeKnora.git`
- Baseline commit: `350d9e4e`
- Baseline describe: `v0.7.1-147-g350d9e4e`

Sync history: W0 established `5780affd` (`v0.7.1-115`), on which everything recorded for W1, W2a0 and W2a1 was measured. `main` was fast-forwarded to `ccb0cee3` (`v0.7.1-126`) mid-W2a1 without merging into `researchflow-ext`, after checking that the 11 new commits touched none of the W2a1 blast radius. At the W2a2 opening sync `main` advanced again to `82ea91f1` and was merged into `researchflow-ext` (merge `bd097eb6`, no conflicts, 13 upstream commits across 28 files); W2a1's build and tests were re-verified on the merged tree. At the W3 opening sync `main` advanced to `350d9e4e` and was merged into `researchflow-ext` (merge `149791f4`, no conflicts, 57 files, 6206 insertions). That merge was checked against the one thing W3 depends on: `resolveDocReader` is unchanged, and upstream's only edit to `knowledge_process.go` was `cloneKnowledge` gaining `FolderPath`.

## Branch discipline

- `main` only mirrors `upstream/main`; no ResearchFlow-specific commit belongs on `main`.
- Synchronize `main` with `git fetch upstream` followed by `git merge --ff-only upstream/main`.
- Before each W-track substage, synchronize `main`, then merge the synchronized `main` into `researchflow-ext`.
- Keep extensions on connector, parser, provider, or DTO plugin surfaces whenever possible.
- Before changing a core pipeline, add the proposed divergence to this ledger and record in a ResearchFlow ADR why the plugin surface is insufficient.
- Evaluate every customization for contribution to upstream and record expected resynchronization impact.

## Entry requirements

Each entry must record its W-track stage, scope, reason, changed files, tests, upstream contribution suitability, and expected impact when resynchronizing with upstream.

## Customization entries

| ID | Stage | Scope | Reason | Files | Tests | Upstream contribution | Resynchronization impact |
|---|---|---|---|---|---|---|---|
| W1-001 | W1 | test-only (`chunker`) | ResearchFlow re-reads every hit at its own range before it may be cited, so it depends on one chunker invariant: `0 <= Start < End <= runeLen(text)` and `Content` ends with `text[Start:End]`. `mergeUnits` prepends a table header outside the range on purpose, so equality is not the contract and the weaker suffix form is what callers can rely on. Pinned across six document shapes × five strategies so a chunker change cannot silently move offsets away from the content they address. | `internal/infrastructure/chunker/chunk_span_contract_test.go` | `go test ./internal/infrastructure/chunker/ -run TestChunkSpanContract` | Suitable — no ResearchFlow concept appears; it states an invariant the package's own doc comment already claims. | Low — test-only, no production symbol touched. Fails loudly if upstream changes offset semantics, which is the point. |
| W1-002 | W1 | test-only (`service`) | `buildSearchResult` is the single crossing where a chunk's range and its knowledge's ingest metadata enter the API shape. Governed retrieval cannot verify a quote without both surviving verbatim, so the passthrough is pinned rather than the wiring. Also pins that synthesized chunks (summary/FAQ/table-summary) keep their empty `[0,0)` range: a consumer can only refuse what it can see, and an empty range widened to the body would read as an anchor. | `internal/application/service/knowledgebase_search_results_span_test.go` | `go test ./internal/application/service/ -run TestBuildSearchResult` | Suitable — DTO passthrough test with no ResearchFlow-specific keys asserted by name beyond generic metadata. | Low — test-only. |
| W1-003 | W1 | test-only (`chat_pipeline`) | Regression lock for the reason governed retrieval must not use `/knowledge-search`: `mergeSequentialChunks` joins neighbouring bodies and deliberately leaves `StartAt`/`EndAt` where they were, so a merged result can no longer be read back at its own range. Turning that documented decision into an executable assertion keeps it from being quietly reverted, and makes the endpoint choice in ADR-0010 verifiable rather than asserted. | `internal/application/service/chat_pipeline/merge_overlap_span_test.go` | `go test ./internal/application/service/chat_pipeline/ -run TestMergeSequentialChunks_LeavesTheRange` | Suitable — documents existing intended behaviour. | Low — test-only. If merging ever starts maintaining the range this test fails, which is the signal to revisit the endpoint restriction. |
| W2a0-001 | W2a0 | test-only (`types/interfaces`) | ADR-0011 routes the discovery profile (`NeedContent`/`NeedUrl`/`Sites`/`BlockHosts`/`AuthInfoLevel`/`TimeRange`/`QueryRewrite`/`ContentFormats`/`Industry`) through a shared vendor client rather than through `WebSearchProvider`, whose `Search(ctx, query, maxResults, includeDate)` cannot carry it. Widening the interface would recompile nine implementations and two call sites for one consumer; smuggling options through `context` would be invisible to the compiler and would fail open — a dropped profile means the vendor is asked for page bodies. The decision only holds if the interface stays put, so its method set is pinned by reflection: an added method, a widened signature, or a variadic option parameter fails the test. | `internal/types/interfaces/web_search_provider_contract_test.go` | `go test ./internal/types/interfaces/ -run TestWebSearchProviderContract` | Suitable — asserts the interface as currently documented; no ResearchFlow concept is named in an assertion. | Low — test-only. A failure is the signal to revisit ADR-0011, not to update the assertion. |
| W2a0-002 | W2a0 | test-only (`types`) | A discovery manifest materializes onto one `data_sources` row and relies on four properties of the existing schema that were never asserted together: policy in `Settings` survives in clear (so an agent can diff the row against the version-controlled manifest), the vendor key in `Credentials` does not (AES-256-GCM at rest), the profile triple `search_type`/`need_content`/`need_url` returns typed rather than stringified (a `"false"` string reads as truthy and would start requesting vendor bodies), and `BeforeCreate` preserves a client-supplied deterministic id (without which re-materializing forks a second data source and doubles every saved query into one inbox). Also pins the JSON key `last_schema_hash` — declared upstream but referenced nowhere — which ResearchFlow uses to reset cursors on a manifest edit. | `internal/types/discovery_manifest_projection_test.go` | `go test ./internal/types/ -run TestDiscoveryManifestProjection` | Partly — the encryption-split and typed-round-trip assertions are generic, but the fixture is ResearchFlow-shaped and would need neutralizing first. | Low — test-only, no production symbol touched. |
| W2a1-001 | W2a1 | new package (`web_search/volcengine`) | The Doubao (Volcengine) Custom search API has two consumers with incompatible shapes: the registered `WebSearchProvider` (chat search, `Search(ctx, query, maxResults, includeDate)`) and the ResearchFlow discovery connector (a typed profile from a version-controlled manifest). ADR-0011 §4 routes both through one client so the 2-method interface pinned by `W2a0-001` never has to grow a discovery profile. The client is a package rather than a file in `web_search` so the connector can import it without pulling in the provider registry, and it takes an injected `*http.Client` so it never decides outbound network policy (importing `web_search.NewSearchHTTPClient` would be an import cycle). Three properties are structural, not conventional: `DiscoveryProfile` has no field for `SearchType`/`NeedContent`/`NeedURL`, so a profile that asks the vendor for page bodies cannot be expressed; `APIError` carries no vendor `Message`, because `10400 ParamError` quotes the offending input and printing it would put the query in a log line; and options are validated locally before the request, so a malformed profile fails without provoking that message. Business errors ride inside HTTP 200 (`ResponseMetadata.Error`), so status alone never decides success, and a 200 with `Result == nil` is refused rather than reported as an empty page. | `internal/infrastructure/web_search/volcengine/client.go`, `internal/infrastructure/web_search/volcengine/errors.go` | `go test -race ./internal/infrastructure/web_search/volcengine/` (22 tests, `httptest` fake server, zero network) | Partly — the client and its error taxonomy are generic and would suit upstream, but `DiscoveryProfile` and the no-`Message` rule exist for ResearchFlow governance and would need a rationale upstream does not currently have. | Low — new package, no upstream file touched. Vendor-side changes (new error codes, a revived `web_summary` edition) land here and nowhere else. |
| W2a1-002 | W2a1 | provider registration (`web_search`, `types`, `service`, `container`, generated docs, frontend) | Registers `volcengine` as the tenth web search provider so the shared client is reachable from the existing per-tenant encrypted-credential path instead of a second configuration mechanism. The provider holds no request options of its own and publishes **no** `ConfigFields`: the same vendor is also driven by a version-controlled discovery manifest, so a request option editable per tenant in the settings UI would be a policy the project repository no longer describes. It never sets `NeedContent` — a vendor-side snapshot is not the document a governed citation can point at — and it logs only counts, deliberately not copying the `query=%q` line at `keenable.go:80`. Generated Swagger artifacts are hand-edited to match what `swag init` emits for the widened enum, since the generator is not run in this environment. | `internal/infrastructure/web_search/volcengine.go` (new), `internal/infrastructure/web_search/test_errors.go`, `internal/types/web_search_provider.go`, `internal/application/service/web_search_provider.go`, `internal/container/container.go`, `docs/swagger.json`, `docs/swagger.yaml`, `docs/docs.go`, `frontend/src/api/web-search-provider.ts`, `frontend/src/views/settings/WebSearchSettings.vue` | `go test -race ./internal/infrastructure/web_search/... ./internal/types/... ./internal/application/service/...`; provider projection covered through a `volcengineSearcher` fake, no second HTTP server | Suitable — a provider addition is exactly the shape this surface is designed for. The absent `ConfigFields` is the only ResearchFlow-specific choice and is defensible on its own (the vendor's options are all defaulted). | Medium — touches six shared files by one line or one case each, plus three generated docs. Conflicts are mechanical; a regenerated `swagger.json` upstream would resolve by regenerating. |
| W2a2-001 | W2a2 | new package (`datasource/connector/discovery`) | The discovery connector turns saved queries into inbox candidates. It is the first connector whose body does not come from the system it queries: the vendor supplies identity (title, URL, publish time) and the *original page*, fetched first-party through the SSRF-safe client, supplies the content. Nothing the vendor said about the content — snippet, summary, authority tier, relevance score — is carried, because a downstream reader could not tell it apart from the page's own text, and a content hash taken over a vendor snapshot would not match the page. Three failure granularities are distinguished on purpose: a candidate that fails to fetch is left out of the new cursor so the next run retries it; a query whose search fails carries its prior progress forward untouched and reports `PartialFetchError`; every query failing returns a **nil** cursor, because the sync service persists any non-nil cursor even on a fetch error and would otherwise mark every candidate as seen after a total outage. An oversized page is refused rather than truncated. `Validate` makes no vendor call — validating by running the saved queries would spend quota, 500 calls a month on the free tier, every time the settings drawer opens. | `internal/datasource/connector/discovery/{config,page,cursor,connector}.go` | `go test -race ./internal/datasource/connector/discovery/` (49 tests, `httptest` for the page gate, typed fakes for the vendor seam, zero network); each of the three failure granularities and both leak assertions verified non-vacuous by mutation | Partly — the connector shape and the SSRF page gate are generic, but "the vendor body is never the content" is a ResearchFlow governance rule that upstream has no reason to adopt, and it is the reason for most of the design. | Low — new package, no upstream file touched. |
| W2a2-002 | W2a2 | connector registration (`types`, `datasource`, `container`) | Registers the connector so a materialized manifest is syncable: `ConnectorTypeDiscovery = "discovery"` (the string ResearchFlow writes into `DataSourceConfig.Type`, pinned by `W2a0-002`), `ChannelDiscovery` for candidate provenance, a `ConnectorMetadataRegistry` entry that advertises `incremental` but deliberately **not** `deletion_sync` — a hit leaving a vendor's ranking says nothing about the page behind it — and one `initConnectorRegistry` line. | `internal/types/datasource.go`, `internal/types/knowledge.go`, `internal/datasource/connector.go`, `internal/container/container.go` | `go test -race ./internal/datasource/ -run TestDiscoveryMetadata`; `go build ./internal/...` | Suitable — a connector addition is the shape this surface is designed for. | Low — four shared files, one constant or one line each. |

| W2b1-001 | W2b1 | new package (`academic_search`) | The vendor-neutral base the four registry clients project onto. It sits beside `web_search` rather than inside it because the two lanes differ on the only property that matters: a web hit is followed by a first-party fetch that becomes the governed body, an academic hit is not (ADR-0012 §2). `Work` has no field for an abstract, a snippet or a body, and `contract_test.go` pins that by reflection against a body-shaped-name list plus an exact expected field set, so widening any of `Work`/`Response`/`Options`/`APIError` fails a test rather than passing review. `Options` has no `NeedContent` and no way to express one. `APIError` carries no vendor message, because three of the four registries echo the offending request in their error bodies and the saved query may not reach a log line (ADR-0009 §9); `403` is deliberately terminal, since on Crossref it is a manual block by a human and retrying into it is what earned it. `NormalizeDOI` refuses rather than repairs — a mangled deduplication key is worse than an absent one, and its allowlist is deliberately narrower than DOI syntax permits so it can only ever be widened, never corrected. `Pacer` exists because the per-registry rate promises are deployment obligations, not settings: arXiv's is a fleet-wide total, PubMed's is tied to an offline registration, so both live as client constants where no manifest can edit them (ADR-0012 §5). | `internal/infrastructure/academic_search/{academic,errors,pacer,retry}.go` | `go test -race ./internal/infrastructure/academic_search/` (17 assertions incl. subtests, no network, no clock: the pacer takes an injected clock and sleep) | Partly — the pacer, the retry helpers and the error taxonomy are generic. The identity-only shape and its reflection guards exist for ResearchFlow governance and would need a rationale upstream does not have. | Low — new package, no upstream file touched. |
| W2b1-002 | W2b1 | new package (`academic_search/arxiv`) | arXiv is the one registry that both holds the full text and forbids serving it from our servers, so the identity-only shape is the only compliant shape here rather than a governance preference. The Atom entry struct declares no `summary` field, so `encoding/xml` discards the abstract instead of a later filter someone could remove. Two arXiv-specific decisions: `<arxiv:doi>` is **not** adopted as the candidate's DOI — it is the author's declaration of where the work was later published, and ADR-0012 §3 decision 2b holds the preprint and the version of record to be two Documents, so adopting it would file an e-print PDF under the journal article's canonical key; and the version suffix is stripped from the e-print id, because it names a revision, not a work, and revision-level difference is settled at promote time by content hash. **Revised in W2b2 per ADR-0013 §4**: the client now also *derives* arXiv's own DataCite DOI, `10.48550/arXiv.<unversioned id>`, keeping `ArXivID` alongside it. W2b1 declined this on the grounds that it would assert an unobserved identifier — correct while unverified, and no longer so: arXiv publishes the derivation as a construction rule, the 1991–2021 backfill completed in February 2022 so coverage is total, and there is one DOI per e-print rather than per version, which makes the version stripping above the correct *input* to the derivation rather than a lossy step before it. The derived string goes through the shared `NormalizeDOI` rather than being formatted and trusted, so an id that cannot form a well-formed DOI yields no DOI instead of a corrupt deduplication key. This is what makes an arXiv candidate and the same preprint reached through OpenAlex land on one canonical key, with no same-work edge added to deduplication (which ADR-0012 §3 forbids). The journal `<arxiv:doi>` is still not adopted. Errors arrive as HTTP 200 Atom feeds, so status alone never decides success, and the error code is a constant rather than arXiv's error-id fragment, which echoes the request. A profile that excludes preprints skips the request entirely: everything here is a preprint, so the answer is known without spending one of a three-second budget. | `internal/infrastructure/academic_search/arxiv/client.go` | `go test -race ./internal/infrastructure/academic_search/arxiv/` (23 assertions incl. subtests, `httptest` fake server, zero network); log-leak assertion covers the success, error-feed and transport-failure paths with a non-vacuity guard | Partly — the client is generic; the refusal to adopt `<arxiv:doi>` is a ResearchFlow governance decision. | Low — new package. Registry-side changes land here and nowhere else. |
| W2b2-001 | W2b2 | new package (`connector/querycursor`) + refactor of `connector/discovery` | The saved-query sync state, extracted so the academic connector can share it (ResearchFlow ADR-0012 §6). What justifies a package rather than a copy is one subtlety: a sync has three failure granularities that are not interchangeable, and each is now a *different* operation — a failed candidate is simply never `Record`ed so it retries; a failed search calls `CarryQueryProgress` so that query does not re-emit what it already sent; and a wholly failed run is expressible only by the caller returning no cursor at all, which this package deliberately cannot say, because the sync service persists any non-nil cursor even on a fetch error. `StartQuery` exists so an empty slot means "ran, found nothing new" rather than "never ran". `CarryQueryProgress` clones rather than aliases. `Fingerprint` is content-agnostic — the web lane hashes the page Markdown, the academic lane will hash its rendered card — which is also why that card must render deterministically. `page.go` is untouched; the academic lane never fetches a page. | `internal/datasource/connector/querycursor/cursor.go`, `internal/datasource/connector/discovery/connector.go` (call sites), `internal/datasource/connector/discovery/cursor.go` (deleted) | `go test -race ./internal/datasource/connector/querycursor/` (8 tests) plus `./internal/datasource/connector/discovery/` (69 assertions, **unchanged** — no discovery test referenced the cursor internals, so they are the regression proof for the extraction) | Partly — the three-granularity state machine is a generally useful connector pattern, but its rationale is ResearchFlow's. | Low — new package; the only edited file is one already-diverged connector. |
| W2b2-002 | W2b2 | two constants (`types`) | `ConnectorTypeAcademic` and `ChannelAcademic`. The academic lane is a second discovery lane with the opposite answer to the only question that matters downstream: whether a candidate carries a body. Sharing `"discovery"` for both would put "candidate carries the original page" and "candidate carries identity only" behind one runtime value, and "did anything ingest text it was not permitted to" would stop being a compile-time property. Both strings are contract: ResearchFlow materializes its academic manifest onto a row with exactly this Type. The projection test also pins that the materialized academic settings contain **no** body-shaped or web-lane key (`need_content`, `need_url`, `search_type`, `content_formats`, `industry`, `sites`, …) and that `identity_only` survives the round trip **as a bool** — a bool returning as the string `"false"` would read as truthy in a careless connector. | `internal/types/datasource.go`, `internal/types/knowledge.go`, `internal/types/academic_manifest_projection_test.go` | `go test ./internal/types/ -run Academic` (3 tests) | Suitable — a connector-type addition is the shape this surface is designed for. | Low — one constant each in two shared files. |
| W2b2-003 | W2b2 | new package (`connector/academic`) | The identity-only connector defined by ADR-0013. Its `Connector` struct has exactly one capability seam — `newSearcher` — and a reflection test pins that field set, so adding a page fetcher, URL client or body endpoint is a structural contract change rather than a convenient helper. Every registry record becomes a deterministic bibliographic card containing only candidate-layer claims (title, authors, venue, year, identity, registry type) and an explicit notice that it contains neither the work's text nor abstract. The card's non-empty `Content` is the **mechanism** that makes WeKnora's `CreateKnowledgeFromURL` branch unreachable: the sync service takes its content-from-bytes branch and returns first. Cards collapse every registry string to one line so metadata cannot break out into a Markdown section that looks like body text; fixed field order, no timestamps and no map iteration make the bytes stable because the shared cursor fingerprints them. The candidate URL is the strongest identity's HTTPS resolver: doi.org (folds to the RF `doi:` key), PubMed for PMID-only records, and canonical arXiv abs only if derived DOI validation fails. The last two must be passed unchanged to `promote-file`; a later DOI for a PMID-only work is an identity upgrade for a human to judge, not a dedup fact. Config parsing is exact in both directions: the provider/profile/contact/key shapes from ADR-0012 are reasserted; any web/body setting is rejected rather than ignored; a bool-string `identity_only` is rejected; query text and credentials appear in no config error, log, list response or metadata. Validate makes no registry call. Saved queries run serially; the clients' fixed pacers remain the sole rate authority. Three failure granularities go through `querycursor`: failed card unrecorded/retried, failed query carries progress/partial, all queries fail → nil cursor + hard failure. Metadata carries identity and stable reference but no query/abstract/snippet/landing page. | `internal/datasource/connector/academic/{config,card,connector}.go` | `go test -race ./internal/datasource/connector/academic/` (57 assertions incl. subtests, typed fake searcher, zero network); `go vet` clean | Partly — the connector and registry fan-out are generally useful; the bibliographic-card mechanism and the manifest-exact fail-closed profile are ResearchFlow governance. | Low — new package, no upstream file touched. |
| W2b2-004 | W2b2 | connector registration (`datasource`, `container`) | Registers `ConnectorTypeAcademic` so the materialized row reaches the connector. Metadata advertises `incremental` and deliberately no `deletion_sync`: a work leaving registry ranking is not a retraction signal, and deleting its card would erase review state based on ranking movement. `AuthType = "mixed"` is honest about the four typed profiles (anonymous arXiv, keyed OpenAlex/PubMed, contact-only Crossref polite, contact+token Plus) rather than pretending one UI credential shape describes them; project manifests remain the policy authority. | `internal/datasource/connector.go`, `internal/datasource/connector_test.go`, `internal/container/container.go` | `go test -race ./internal/datasource/ -run Academic`; `go vet ./internal/datasource`; `internal/container` compile reaches upstream `sqlite-vec` cgo and needs `libsqlite3-dev` in the image, per the existing W2a1 environment note | Suitable — a connector registration is the plugin surface this code is designed for. | Low — one metadata entry, one registry line and one import. |
| W2b1-003 | W2b1 | new package (`academic_search/crossref`) | Crossref's own carve-out — "Some abstracts contained in the metadata may be subject to copyright by publishers or authors" — is what removed the last uniform footing under treating a registry abstract as governed text, so this client names the identity fields it wants in `select` and the abstract never crosses the wire. Three decisions are structural: the anonymous public pool is **not offered**, because Crossref contacts a misbehaving caller before blocking it and an anonymous caller gave them nobody to contact (ADR-0012 §5), so only `polite` and `plus` exist; the pool the registry reports in `x-api-pool` is compared against the configured one and a lower pool is refused, which converts the silent downgrade ADR-0012 §5 gap 1 warns about — and the header-name inconsistency in Crossref's own docs recorded in §9 item 3 — into a loud non-retryable failure instead of unexplained slowness; and a Plus token supplied for the polite pool is refused rather than ignored, because a credential that will never be sent is a paid subscription silently going unused. Filters Crossref cannot express are refused, not dropped: there is no review-article type (`peer-review` is a review *of* a work) and no open-access filter (`has-license` is a different question wearing this one's name), and answering such a profile with an unfiltered reading list would look like an answer. `x-rate-limit-*` and `x-concurrency-limit` are recorded because a limit below the pool's documented one is how a throttle announces itself before it becomes a block. | `internal/infrastructure/academic_search/crossref/client.go` | `go test -race ./internal/infrastructure/academic_search/crossref/` (26 assertions incl. subtests, `httptest`, zero network) | Partly — the pool-downgrade check is a generally useful idea; the refusals are ResearchFlow governance. | Low — new package. |
| W2b1-004 | W2b1 | new package (`academic_search/openalex`) | OpenAlex ships the abstract as `abstract_inverted_index` — word to positions — which reconstructs verbatim with a sort. It is the reason a body-shaped-field-*name* guard is not sufficient anywhere in this tree: the field is not called "abstract". Three defences in order of strength: it is absent from `select`, so the server never sends it; the wire struct declares no field it could decode into (pinned by a recursive reflection walk in the test); and `Work` has none either. An API key is **required** rather than optional — ADR-0012 §5 gap 1 and §8 — because a key-less caller is metered at a tenth of the daily budget and the failure presents itself as "nothing was published on this topic" rather than as an error. The key travels as a query parameter (OpenAlex offers no header form), so no log line here may contain a request URL, and transport errors are unwrapped to their innermost cause because `*url.Error` stringifies with the URL in it. Work types are mapped only where OpenAlex documents a value: its `type` has **no enum** in the OpenAPI spec, only six named "common values", and filtering on a term it does not use matches nothing and reads as an empty topic. `type:` values are OR-joined with a pipe, since a comma is AND and no work has two types. | `internal/infrastructure/academic_search/openalex/client.go` | `go test -race ./internal/infrastructure/academic_search/openalex/` (20 assertions incl. subtests, `httptest`, zero network) | Partly — the inverted-index refusal is specific to ResearchFlow's governance boundary. | Low — new package. |
| W2b1-005 | W2b1 | new package (`academic_search/pubmed`) | The endpoint choice *is* the boundary: E-utilities distinguishes ESearch (UIDs), ESummary (document summaries, no abstract) and EFetch, which "can produce abstracts from Entrez PubMed". This client is ESearch then ESummary and has no code path reaching EFetch, so the abstract is never requested rather than filtered out — a test asserts the exact set of endpoints called. `querytranslation`, which echoes the saved query back expanded, has no field in the wire struct, so it cannot reach a log line even by accident. An API key, a registered `tool` and a registered `email` are all required (ADR-0012 §8): key-less means 3 rps and, unregistered, a blocked IP, and NCBI's stated remedy for a caller it cannot contact is to block the address — so both requests carry the identity, not just the first. `mindate`/`maxdate` are documented as a pair, so a half-open window gets a synthetic far endpoint; sending one alone would be an ignored filter, which silently widens the search. The rate-limit envelope arrives inside an HTTP 200 and is classified retryable, unlike a rejected term. The `[pt]` map holds four entries and fails closed on the rest. It held three until 2026-08-05, because the MeSH publication-type list was unreachable during implementation and ADR-0012 §9 forbids coding an unverified value from prose; reading that list (NLM "Publication Characteristics with Scope Notes", reviewed 2026-07-01) settled all three open kernel types at once — `Dataset` is in the vocabulary and was added on that citation, while `book-chapter` has no counterpart and `conference-paper`'s nearest terms name the volume (`Conference Proceedings`) or a single abstract (`Meeting Abstract`) rather than a paper, so both stay refused on a citation instead of on an unreachable page. What the list cannot settle is how many records carry a type; only an authorized request could. | `internal/infrastructure/academic_search/pubmed/client.go` | `go test -race ./internal/infrastructure/academic_search/pubmed/` (28 assertions incl. subtests, `httptest` routing on script name, zero network) | Partly — the two-call identity discipline is generic; the EFetch abstention is ResearchFlow governance. | Low — new package. |
| W3-001 | W3 | test only (`application/service`) | A certification test, not a divergence: it pins the resolution ResearchFlow's locators already depend on. A governed revision is uploaded as `<document_id>-r<revision>.md` with no parser engine named, so `ResolveParserEngine` yields `""`, `resolveDocReader` takes its default branch, `IsSimpleFormat("md")` holds, and `SimpleFormatReader` returns the bytes verbatim. That verbatim return is what makes a char offset — and, after ResearchFlow ADR-0014, a cell coordinate — address the same characters in the retrievable copy as in the governed one. The whole of docreader's table machinery (`excel_parser.py`, the DuckDB/LLM table and column descriptions, markitdown's csv path) is therefore **structurally unreachable** for governed documents, which is why W3's table work landed in the ResearchFlow kernel instead of here. Nothing in this repository is changed; if upstream ever routes `md` through docreader, this test fails, and it must, because docreader rewrites images, normalizes tables and reflows text — each of which moves an offset that is already recorded in a governance record. No table span test was added: `chunk_span_contract_test.go` (W1-001) already covers `single_table`, `two_tables` and `headings_and_table` across five strategies. | `internal/application/service/governed_markdown_reader_test.go` | `go test ./internal/application/service/ -run "GovernedMarkdown\|SimpleFormatReader"` (2 tests, 3 subtests, zero network) | Not applicable — the test encodes a ResearchFlow dependency on upstream behaviour, not a change upstream needs. | None — test-only file; it fails if and only if the resolution it certifies changes. |
| R1-001 | R1 (post-W) | product (`web_search` multi-source) | Upstream `WebSearchService.Search` is single-provider. R1 dual-source discovery (Tavily + Volcengine) needs fan-out plus rank fusion as a product path, not a project-side probe: vendor relevance scores are not commensurate, so the only honest merge is Reciprocal Rank Fusion over ranks (`k=60`). Agent/config may declare `web_search_provider_ids` (≥2); `Search` then fans out in parallel (cap 4), tolerates partial failure when ≥1 provider succeeds, fuses by normalized URL (scheme+host+path, drop query/fragment), rewrites `Source` to a sorted comma-joined provider-type set, and applies the existing blacklist after fusion. Single-provider callers and a one-element list stay on the classic path. Logs only counts (`providers`/`ok`/`query_len`), never the query text. | `internal/application/service/web_search_fuse.go` (new), `web_search_fuse_test.go` (new), `web_search.go`; `internal/types/{web_search,agent,custom_agent,chat_manage}.go`; `internal/agent/tools/web_search.go`; `internal/application/service/{agent_service,session_agent_qa,session_knowledge_qa,agent_share}.go`; `internal/application/service/chat_pipeline/search.go`; `client/agent_manage.go` | `go test ./internal/application/service/ -run 'TestFuse\|TestNormalize\|TestDedupe'` (4 tests, zero network); compile-checked `./internal/application/service/`, `./internal/agent/tools/`, `./internal/types/`, `./internal/application/service/chat_pipeline/` | Suitable — multi-provider RRF is a generic product shape; the ResearchFlow-specific part is only the opt-in field name and the discovery use case. | Medium — touches the shared `Search` entry and agent config DTO. Conflicts are mechanical (one field + one multi branch). Does not widen `WebSearchProvider.Search` (ADR-0011 contract stays). |
| R1-002 | R1 (post-W) | product (`chat_pipeline` web_fetch) | Upstream `PluginWebFetch` only reads `RerankResult`. When rerank is skipped (empty `rerank_model_id`) or yields no web hits, `RerankResult` stays empty and `web_fetch` no-ops even though `WebFetchEnabled` is true and `SearchResult` already holds `knowledge_source=web_search` rows — the same condition under which `PluginMerge` already falls back to `SearchResult`. R1 dual-web agents often run without a working rerank endpoint; enrichment must not require one. Fix: `selectWebResultsForFetch` prefers rerank web hits, else falls back to search web hits (cap `WebFetchTopN`, default 3); logs `source=rerank|search` and only counts, never query text. Pointers alias pipeline slices so content writes stick for Merge. | `internal/application/service/chat_pipeline/web_fetch.go`, `web_fetch_select_test.go` (new) | `go test ./internal/application/service/chat_pipeline/ -run 'TestSelectWebResultsForFetch'` (5 tests, zero network) | Suitable — fallback mirrors Merge's existing input selection; ResearchFlow only surfaces the gap because R1 agents clear broken rerank placeholders. | Low — one pipeline plugin; selection helper is pure. |
| R1-003 | R1 (post-W) | provider registration (`web_search`, `types`, `service`, `container`, generated docs, frontend) | Adds `serpapi` and `perplexity` as web search provider types so multi-source RRF can bind Google web, Google Scholar, and (when keyed) Perplexity citations alongside Tavily/Volcengine. SerpAPI pins `engine` via ExtraConfig (`google` / `google_scholar`) so one type yields two tenant instances; endpoint is hardcoded (`https://serpapi.com/search.json`) to prevent SSRF. Perplexity uses chat-completions (`sonar` default) and projects citation URLs into `WebSearchResult` with vendor answer truncated to a snippet — `Content` stays empty (vendor synthesis is not governed body; no citations → empty list). Its proxied upstream intermittently closes a stream before response headers or before completing the body, so only those transport/incomplete-body failures receive one context-bounded retry on a fresh connection; HTTP statuses and oversized responses never retry. SerpAPI request credentials travel in the query string, so transport wrappers and vendor response details are replaced with typed, query-free summaries before errors reach shared multi-source logs. Logs only engine/model/counts, never query text. `MaxWebSearchAggregateProviders` raised 4→5 so tavily+volcengine+serpapi-google+serpapi-scholar+perplexity fit one agent. Swagger enums hand-edited. Plugin code only; live register of tenant entities is an R1 ops step outside this ledger. | `internal/infrastructure/web_search/{serpapi,perplexity}.go` (+ tests), `internal/types/web_search_provider.go`, `internal/application/service/{web_search_provider,web_search_fuse}.go`, `internal/container/container.go`, `docs/{swagger.json,swagger.yaml,docs.go}`, `frontend/src/api/web-search-provider.ts`, `frontend/src/views/settings/WebSearchSettings.vue` | `go test ./internal/infrastructure/web_search/` (httptest, zero network; includes transport EOF/incomplete-body retry and non-retryable HTTP assertions); fuse cap covered by existing `TestDedupeProviderIDs` | Suitable — provider addition is the designed extension surface. Cap raise is a product constant, not RF-specific. | Medium — two new types + shared enum surfaces; conflicts with regenerated swagger are mechanical. |

## W3 notes

- **The stage's opening assumption did not survive its own audit, for the third time on this track.** Plan v2 §11 called W3 "docreader table/CSV chunking and cell locator". The read-only audit found that governed bodies never enter docreader at all (W3-001 above), so nothing in this repository could have delivered the acceptance criterion. The kernel-side work — deterministic CSV→GFM conversion and the `cell:` locator — is recorded in ResearchFlow ADR-0014.
- **The fork's table capability is real but is not governed text.** `extract.go` builds table summaries and column descriptions with an LLM, and `isSearchableChunk` does include them in results. They carry no `Start`/`End`, so ResearchFlow ADR-0010's `end > start` gate refuses them on arrival. That is the correct outcome and needed no change here: a generated description of a table is not a passage of the document.
- **ADR-0010 §5's two open limits were re-reviewed at W3.** §5.1 (parent-child lets a child chunk exceed its parent's span on documents containing tables) still holds, and governed knowledge bases keep `enable_parent_child = false`. §5.2 (image parsing rewrites markdown before chunking) is **void for the governance lane**, because that rewriting lives in the Python pipeline a governed `.md` never reaches; ResearchFlow ADR-0014 §6 replaces it with a narrower precondition — a governed knowledge base must declare no `ParserEngineRule` for `md`, since naming an engine is precisely how a governed upload would be routed away from `SimpleFormatReader`.

## W2b1 notes

- **No live call was made to any of the four registries.** Every request shape, envelope, field name and limit assertion comes from official documentation replayed against an `httptest` server. Real calls need explicit user authorization plus a governed project ADR (ADR-0012 §10), so live behaviour remains unverified by construction.
- **ADR-0012 §9 item 2 is now resolved.** Verified against `developers.openalex.org` on 2026-08-03: the current OpenAlex model is key-only, the key is sent as the `api_key=` query parameter with no header form, and there is **no polite pool and no `mailto` convention**. Content download is metered separately from list and search, which is one more reason this lane never downloads anything. Also confirmed: `doi` is returned as a resolver URL (so `NormalizeDOI`'s prefix stripping is the common path, not an edge case), `abstract_inverted_index` is documented as "the abstract as an inverted index (word positions)", and `per_page` — with an underscore — caps at 100.
- **ADR-0012 §9 item 5 is confirmed and remains a deployment precondition.** NBK25497 is explicit that supplying `tool` and `email` in requests is not registration: "these values must be registered with NCBI", and an unregistered IP that breaches policy is restored only after registration and email validation. No code can establish this.
- **Two verification gaps are left fail-closed rather than guessed.** OpenAlex's `type` has no enum in its OpenAPI spec (six documented "common values"), and PubMed's `[pt]` vocabulary page was network-blocked during implementation. Both clients therefore map only the terms they can cite and refuse the rest, so widening either map is a documentation task with a citation, not an edit. The cost is a filter a manifest could have had; the alternative cost is a filter that matches nothing and reads as an empty topic.
- **A cross-provider identity question is open for W2b2, and it is a contract question, not a bug.** arXiv's Atom API exposes no DataCite DOI, so an arXiv-only candidate's strongest identity is `arxiv:2401.00001` — and `arxiv:` is **not** a form `canonicalize_reference` accepts (ADR-0012 §3 adds only `doi:`). The same preprint reached through OpenAlex arrives as `doi:10.48550/arxiv.2401.00001`, a form that does canonicalize. So the connector needs a decided answer for what canonical reference an arXiv-only candidate carries into the inbox, and deriving the DataCite DOI from the e-print id would be asserting an identifier we never observed. Per ADR-0012's own sequencing — audit before contract, contract before code — this belongs in W2b2's contract step.
- **Retry classification is uniform and deliberately narrow across all four:** 429 and 5xx only, `Retry-After` honoured when sent, three attempts. `403` is terminal everywhere — on Crossref it is a human's block, on OpenAlex an exhausted budget, on NCBI a blocked address — and in each case retrying is the behaviour that caused it.

## W2a2 notes

- **`InboxItem.origin` is sufficient; no new ADR is needed.** The W2a0 audit left open whether ResearchFlow's single-string inbox provenance could express *which saved query* produced a candidate. It can: every candidate carries `SourceResourceID = query_id` plus `saved_query_id`, `discovery_source_id`, `discovery_project_id` and `discovery_provider` in `Metadata`, so an origin string can be composed from the row without inventing a field. Composing it is a ResearchFlow-side step, and the real `read_inbox` is still unimplemented there by design (live integration is unauthorized), so W2a3 owns the mapping.
- **The query text never leaves configuration.** It is absent from log lines, from `PartialFetchError` details (which reach the sync log), from parse errors, from `ListResources` output (that response feeds a settings UI over HTTP and this connector does not control whether it is logged), and from candidate metadata. Only `query_id` travels.
- **`Validate` deliberately makes no vendor call**, diverging from the RSS connector, which fetches every feed. Running the saved queries to validate a row would spend quota — 500 calls a month on the free tier — every time someone opens the settings drawer.
- **A candidate is identified by query *and* URL.** Two saved queries finding the same page produce two candidates with distinct provenance rather than one whose origin depends on which query ran last. The duplicate is refused downstream by promotion's canonical-URL and content-hash dedup, which is the gate that exists for it.
- **An oversized landing page is refused, not truncated.** Truncating would still ingest a body, and its content hash would then describe something that is not the page.
- **Three failure granularities, verified by mutation.** A candidate that fails is left out of the new cursor so it retries; a query whose search fails carries its prior progress forward untouched and reports partial; every query failing returns no cursor at all. Mutating each of the three — and the two leak assertions — makes the corresponding test fail.

## W2a1 notes

- **No live call was made.** Every request-shape, field-name and error-code assertion comes from the official Doubao documentation and its curl/Python samples, replayed against an `httptest` server. Calling the real endpoint needs explicit user authorization plus a governed project ADR, so anything about live behaviour remains unverified by construction — including whether `Summary` is actually populated at the documented 500–1000 characters.
- **What W2a2 inherits from the client.** `DiscoveryProfile.Options()` is the only way discovery reaches the wire, and it writes `SearchType`/`NeedContent`/`NeedURL` as constants. `Options.Validate()` is exported so the connector can reject a bad manifest before materialization rather than on the first scheduled search. `Response.Dropped` counts hits refused for lacking an absolute `http(s)` URL — the connector should surface it rather than treat a short page as a complete one.
- **Retry classification is deliberately narrow.** Only `10500` and `700429`, HTTP 429 and HTTP 5xx are retried; an unrecognized business code is terminal, because the free tier is 500 calls a month and retrying an unclassified failure spends quota to learn nothing. `Retry-After` is honoured when sent.
- **Go toolchain in this environment.** `go build ./...` needs `-buildvcs=false` (the container cannot read the repository's VCS state), and any package reaching `sqlite-vec-go-bindings/cgo` needs `libsqlite3-dev` installed in the image first.
- **Two upstream test failures here are environmental, not regressions.** `internal/handler`'s `TestPutTenantParserConfigAdminPreservesRedactedSecrets` PUTs `https://example.com/mineru`, which `validateParserEngineOutboundURLs` rejects because this network resolves the host into `198.18.0.0/15` — the same DNS artifact already noted for the datasource connectors. `internal/application/service`'s `TestTenantAPIKeyServiceAuthenticateThrottlesLastUsedUpdates` fails only under the full-package race run and passes on three consecutive isolated runs.

## W2a0 findings carried into ResearchFlow ADR-0011

Read-only audit on this baseline; no fork production change proposed or made:

- **`DataSourceConfig.Settings` already carries arbitrary discovery options to a connector at zero core cost.** Every `Connector` method receives `*types.DataSourceConfig`, so a typed options struct decoded from `Settings` needs no interface change. `Settings` is untyped, unvalidated and stored in clear, so validation is the connector's obligation — ResearchFlow validates ahead of materialization rather than trusting the row.
- **The cursor is persisted even when a fetch fails hard.** `datasource_service.go` deliberately writes back a non-nil cursor on `fetchErr` so transient outages do not force a full re-ingest. ADR-0009 §9's "cursor does not advance when the whole page fails" is therefore a *connector* obligation — return a nil cursor — not a core guarantee.
- **`web_search_providers` has no UNIQUE constraint on `(tenant_id, name)`** (`migrations/versioned/000030_web_search_providers.up.sql`). Referencing a provider instance by name is ambiguous by construction; a name-based reference must be fail-closed on a uniqueness check, or use the id.
- **Nine web-search providers are registered, not eight.** `registerWebSearchProviders` includes zhipu alongside bing/google/baidu/duckduckgo/searxng/tavily/ollama/keenable. ADR-0009 recorded eight at this same baseline; corrected in ADR-0011.
- **`initConnectorRegistry()` takes no arguments**, and connector constructors such as `rss.NewConnector()` take none either. A discovery connector that needs a vendor client must construct it from its own config rather than receive it — which is what makes the shared-client route a zero-blast-radius change.
- **`UpdateDataSource` force-preserves stored credentials** regardless of the request body, while letting `Type`/`ResourceIDs`/`Settings` through. Re-materializing a manifest therefore cannot clobber the key; idempotent materialization depends on this.
- **`POST /data-sources` accepts a client-supplied `id`** (`handler/datasource.go` binds the request into `types.DataSource` and forces only `TenantID`), which is what makes a deterministic id addressable in the first place.
- **Raw query logging already exists on the provider surface.** `web_search/keenable.go:80` logs `query=%q`. ADR-0009 §9 forbids copying it; W2a1 acceptance must capture logs as a counter-example.

## W1 findings carried into ResearchFlow ADR-0010

Measured on this baseline, test-only, no fork production change proposed:

- **Parent-child splitting loses the range on documents with tables.** `SplitParentChild` re-splits each parent's own `Content` and shifts child offsets by `parent.Start`; that arithmetic is sound only while `parent.Content == text[parent.Start:parent.End]`, which the prepended table header violates. At parent=160/child=60: `single_table` 5 of 10 children unreadable at their range (1 prefixed parent), `two_tables` 5 of 14 (1 prefixed parent), `headings_and_table` 7 of 10 (2 prefixed parents); prose and protected-span corpora 0 broken. Parents themselves always satisfy the contract. Governed knowledge bases must therefore pin `chunking_config.enable_parent_child = false`. Fail-closed on the ResearchFlow side (`locator_mismatch`), so it costs recall on tables, never correctness.
- **Image resolution rewrites markdown before chunking.** `knowledge_process.go` runs `ResolveAndStore` / `ResolveRemoteImages` ahead of splitting, so offsets address the rewritten body when a locally promoted `.md` carries resolvable images. Fail-closed; revisited at W3.
- **Query text reaches logs and langfuse spans on the search path.** Not on the certified `hybrid-search` route's chat pipeline, but real integration must still disable query logging and telemetry before any governed knowledge base is pointed at a live deployment.

## I0-0 proposed divergence (not yet made)

Read-only audit for ResearchFlow's real-world validation plan, unit I0-0. **Nothing in this repository is changed yet.** This section exists because the branch discipline above requires a proposed divergence to be recorded here before the change is made.

- **Proposed (one entry, blocking `source-inbox`): carry a fetched item's publication time into the knowledge row.** `ingestItem` in `internal/application/service/datasource_service.go` builds the metadata map from `ExternalID`, `SourceResourceID`, `DataSource.ID` and the item's own `Metadata`, but `FetchedItem.UpdatedAt` is passed to nothing — `CreateKnowledgeFromFile` has no parameter for it — so the persisted row keeps only its own `created_at`/`updated_at`, which are ingest times. ResearchFlow's `InboxItem.published_at` is required and must be a timezone-aware RFC 3339 value; it feeds the kernel's date-window search filter and the CSL `issued` field of a sealed export, so substituting an ingest time would put a false publication date into a governance record and into citations. The RSS connector already computes the right value (`internal/datasource/connector/rss/connector.go` resolves `UpdatedParsed`, then `PublishedParsed`) and simply does not put it in `FetchedItem.Metadata`; adding that key is a connector-surface change that reaches the row through the existing metadata passthrough, with no core signature touched. Preferred over widening `CreateKnowledgeFromFile`, which nine call sites would feel.
- **Audited and deliberately *not* proposed: the URL-only ingest branch drops all metadata.** When `FetchedItem.Content` is empty, `ingestItem` calls `CreateKnowledgeFromURL` (`internal/application/service/knowledge_create.go`), whose signature accepts no metadata at all, so `external_id`, `source_resource_id` and `datasource_id` are lost. This is not only a ResearchFlow problem: `FindByMetadataKey(ctx, …, "external_id", …)` can never match such a row, so this connector's own update path — delete-then-recreate keyed on `external_id` — silently degrades into re-creation for that branch. Fixing it means widening a core creation signature, which is a larger blast radius than the lane is worth right now: the academic identity cards and full-text RSS items both carry content bytes and take the other branch, so only "watch a URL without extracting a body" is affected, and ResearchFlow is scoping that lane out of its first integration instead. Recorded here so the next person does not rediscover it as a bug.
- **Two facts corrected in ResearchFlow ADR-0002 and ADR-0003.** `GET /knowledge-bases/:id/knowledge` returns `[]*types.Knowledge`, which has **no content field** — a body is read per item from `GET /knowledge/:id/preview`, which streams the stored original. And that list is ordered `created_at DESC` with offset paging while the duplicate-detection path rewrites `created_at` to `time.Now()` on a replay, so the offset is not a usable cursor; the consumer has to build its own stable order over the knowledge id. Both were audited, neither needs a change here.
